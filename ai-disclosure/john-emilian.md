For this sprint, I used ChatGPT for:

- Git workflow (e.g. do I initialize a repo before creating remote, then clone?) order
- asking about how `--add-collaborator` flag for `gh repo edit` is no longer supported (did through broswer)
- setting branch protection for the repo (also through browser)

No generated code or prose was used.
